# CLI tools only. genkeys.php refuses to run over HTTP anyway, but do not even
# let it be reachable.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
