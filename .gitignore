node_modules/
.env
.env.local
# build artifacts & runtime data — never commit (keeps the repo/clone small)
dist/
.next/
*.tsbuildinfo
*.log
update.log
**/uploads/
coverage/
*.ova

# ad-hoc backup/export archives — never commit these; they bloat the repo and
# drift out of sync with source immediately (see ISP_CRM_COMPLETION_ROADMAP.md 0.4)
*.zip
backend-fixed/

# Licence server — MUST NOT be committed to this PUBLIC repo.
# It belongs in its own PRIVATE repository (see docs/LICENSING-PLANE.md).
_LICENCE-SERVER-MOVE-ME-OUT/
# every delivery archive, not just the two named ones
*.tgz
*.tar.gz
*.sk
jbx-config.php
_LICENCE-AGENT-MOVE-ME-OUT/
jbx-agent.tgz
